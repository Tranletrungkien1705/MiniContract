# DEEPEN-LOG — MiniContract

- 2026-XX-XX: BLOCKED — không thể đọc nguồn `D:\idocNet\2020.6.QContract` (tool read trả 403 ngoài workspace, glob từ chối path ngoài workspace) và terminal delegation hỏng hoàn toàn (mọi lệnh, kể cả `Write-Output "probe"`, đều trả `Terminal delegation failed: terminated`). Không chạy được `dotnet build` để verify, không chạy được git. Không port nghiệp vụ nào để tránh over-claim. Cần môi trường có terminal hoạt động để tiếp tục.
- 2026-09-23: Port nghiệp vụ **Phụ lục hợp đồng (annex)** từ nguồn QContract (`FlagContractAnnex` + `ContractRefNo` trong `Contract_Contract.cs`, `SearchContractRefNo` ở Website controller). Thêm `Contract.IsAnnex/ParentContractId/ParentContractCode` + quan hệ tự tham chiếu (DbContext), `ContractService.CreateAnnexAsync/AnnexesAsync` (chặn tạo PL của PL, chặn PL khi HĐ gốc đã hủy), action `CreateAnnex` + view `CreateAnnex.cshtml`, hiển thị danh sách PL ở `Detail`, badge PL ở `Index`, seed 1 PL mẫu + migration cột Postgres. Build Release: 0 error (2 warning cũ ở SignatureService, không liên quan). Commit `b0ca3d9` đã nằm trong `origin/main`.
- 2026-09-23: Port nghiệp vụ **Ô ký trên hợp đồng (Contract_ContractElement)** từ nguồn QContract (`WAS_Contract_ContractElement_Update` + `WAS_Contract_ContractElement_Calc` + model `Contract_ContractElement`/`Contract_ContractElementSum`, hằng `TConst.ElementType` ELECTRONIC/SHORT/DIGITAL). Thêm entity `ContractElement` + enum `ElementType` (Models/Entities.cs), DbSet + mapping (AppDbContext), `ContractService.ElementsAsync/AddElementAsync/SignElementAsync/ElementStatsAsync` + record `ElementStats`, action `AddElement`/`SignElement` + section UI ở `Detail.cshtml` (bảng ô ký, badge loại, thống kê theo loại, form thêm ô, nút ký ô), seed 3 ô ký mẫu + migration bảng `Elements` Postgres. Đồng thời sửa 2 lỗi build **có sẵn** trong HEAD: class `ContractHistory` trùng lặp (xóa bản cũ dùng `HistoryActionType`) và tham chiếu `PublicSign` thiếu controller — tạo `SignController` + 3 view (`PublicSign`/`Invalid`/`Done`) hoàn thiện luồng link ký công khai. Build Release: **0 error, 0 warning**. Verify runtime (SQLite): `/Contract/Detail/3` render section ô ký + seed, POST `/Contract/SignElement` ký thành công (hiển thị tên người ký), `/Sign/PublicSign` render đúng view.
- 2026-09-23: Port nghiệp vụ **Người kiểm tra hợp đồng (Contract_Checker)** từ nguồn QContract (`WAS_Contract_Checker_Accept` + `Contract_Checker_AcceptX` + `Contract_Checker_CheckDB` + model `Contract_Checker`, hằng `TConst.ContractStatus` PENDING/ONPROCESS). Thêm entity `ContractChecker` + enum `CheckerStatus` (Models/Entities.cs), `Contract.CheckerStatus/Checkers/CheckedCount/AllChecked`, DbSet + mapping (AppDbContext), `ContractService.CheckersAsync/AddCheckerAsync/AcceptCheckAsync/CheckerStatsAsync` + record `CheckerStats` (kiểm tra tuần tự theo Idx khi FlagSeq=1, chặn nhảy cóc; đủ người kiểm tra → hợp đồng chuyển ONPROCESS), action `AddChecker`/`AcceptCheck` + section UI ở `Detail.cshtml` (bảng người kiểm tra, badge trạng thái, form thêm, nút kiểm tra), badge KT ở `Index`, helper `Ui.CheckStatus`, seed 2 người kiểm tra mẫu + migration cột `CheckerStatus` + bảng `Checkers` Postgres. Build Release: **0 error, 0 warning**. Verify runtime (SQLite): `/Contract/Detail/3` render section + seed (2 người kiểm tra, "Đã kiểm tra 1/2"), POST `/Contract/AcceptCheck` ký xong → "2/2" + `CheckerStatus=2` (OnProcess) trong DB, thêm 2 người kiểm tra vào HĐ 4 rồi thử kiểm tra người thứ 2 (Idx=2) khi người thứ 1 chưa xử lý → bị chặn đúng (vẫn 0/2). Commit `d49077d` đã nằm trong `origin/main`.- 2026-09-23: Port nghiệp vụ **Lý do kết thúc hợp đồng (Mst_FinishedContractReason) + Kết thúc hợp đồng** từ nguồn QContract (`Mst_FinishedContractReason_CreateX/_UpdateX/_CheckDB` trong `Master.cs`, model `Mst_FinishedContractReason`, hằng `TConst.ContractFinishType` FINISHED/STOPPED, luồng `Contract_ContractParty_FinishX` trong `zTemp.cs` + action `FinishAndSign` ở Website controller). Thêm entity `FinishedContractReason` + enum `FinishType` (Models/Entities.cs), `ContractStatus.Finished` + trường `Contract.FinishReasonCode/FinishReasonName/FinishDescription/FinishedAt` + `IsFinished`, DbSet + mapping (AppDbContext), `ContractService.FinishReasonsAsync/AddFinishReasonAsync/FinishContractAsync` (chặn kết thúc HĐ đã hủy/đã kết thúc, chặn lý do ngừng hiệu lực, ghi nhật ký), action `FinishReasons`/`AddFinishReason`/`Finish` + view `FinishReasons.cshtml`/`Finish.cshtml`, badge "Đã kết thúc" + nút "Kết thúc hợp đồng" ở `Detail`, filter + badge ở `Index`, nav "Lý do kết thúc", helper `Ui.FinishType`, seed 4 lý do mẫu (2 FINISHED/2 STOPPED) + migration bảng `FinishReasons` + 4 cột kết thúc trên `Contracts` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Verify runtime (SQLite): `/Contract/FinishReasons` render 4 lý do seed, POST `/Contract/AddFinishReason` thêm lý do mới OK, `/Contract/Finish/3` render form chọn lý do, POST `/Contract/Finish` kết thúc HĐ 3 → Detail hiển thị badge "Đã kết thúc" + tên lý do + mô tả + thời điểm + dòng nhật ký, `/Contract?status=5` lọc đúng HĐ đã kết thúc, GET `/Contract/Finish/3` trên HĐ đã kết thúc bị chặn (302 + báo lỗi).
- 2026-09-23: Port nghiệp vụ **Phân quyền hợp đồng (Contract_UserInContract)** từ nguồn QContract (`WAS_Contract_UserInContract_Save` → `Contract_UserInContract_SaveX` trong `zTemp.cs`, model `Contract_UserInContract` + `JoinUserInContract` UI model, action `Decentralized` ở Website `Contract_ContractController` + modal `UserInContractSaveModal` trong `Modal.cshtml`). Luật cốt lõi: lưu phân quyền là **GHI ĐÈ TOÀN BỘ** (delete all phân quyền cũ của hợp đồng rồi insert lại danh sách mới). Thêm entity `ContractUserInContract` (Models/Entities.cs) + `Contract.UserAssignments`, DbSet + mapping (AppDbContext), `ContractService.UserAssignmentsAsync/SaveUserAssignmentsAsync` (chuẩn hoá + loại trùng theo UserCode, chặn khi HĐ đã hủy/kết thúc, ghi nhật ký), action `SaveUserAssignments` + section UI ở `Detail.cshtml` (bảng người dùng được phân quyền, textarea nhập `mã | tên | email` mỗi dòng), seed 2 phân quyền mẫu + migration bảng `UserAssignments` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `a23b5a6` (local; push bị remote từ chối do lock ref — chưa nằm trong origin/main). KHÔNG chạy app để verify runtime (theo yêu cầu).- 2026-09-23: Port nghiệp vụ **Lịch sử gửi hợp đồng (Contract_SendHist)** từ nguồn QContract (`WAS_Contract_SendHist_Add` → `Contract_SendHist_SaveX` insert, `WAS_Contract_SendHist_Get` → `Contract_SendHist_GetX` tra cứu theo ContractCode/BulletinType, luồng gửi lại `ContractReSendHist` → `ResendChannel` ở Website `Contract_ContractController`, hằng `TConst.ChannelType` EMAIL/SMS/ZALO + `Client_BulletinType` CONTRACT/OTP). Thêm entity `ContractSendHist` + enum `ChannelType`/`BulletinType` (Models/Entities.cs) + `Contract.SendHistory`, DbSet + mapping (AppDbContext), `ContractService.SendHistoryAsync/AddSendHistAsync/ResendAsync` (ghi nhận gửi theo kênh + bản tin, gửi lại tạo bản ghi mới giữ vết cũ, chặn khi HĐ đã hủy/kết thúc, ghi nhật ký), action `AddSendHist`/`Resend` + section UI ở `Detail.cshtml` (bảng lịch sử gửi, badge kênh/bản tin, form ghi nhận gửi, nút gửi lại các bản tin đã chọn), seed 3 bản ghi gửi mẫu (2 Email CONTRACT + 1 SMS OTP) + migration bảng `SendHistory` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-23: Port nghiệp vụ **Quy tắc đánh số hợp đồng theo loại (Mst_ContractTypeContractNo + Seq_ContractNo_Get)** từ nguồn QContract (`Seq_ContractNo_Get` → `Seq_ContractNo_GetX` trong `Seq.cs`: ghép `{TypefixInput}{số}` khi PREFIX hoặc `{số}{TypefixInput}` khi POSTFIX, số zero-pad theo `SeqNumberLength` qua `To10Mask`; hằng `TConst.Mst_Typefix` PREFIX/POSTFIX; `Contract_Contract_CountByContractType_Get` đếm hợp đồng theo loại để lấy số kế tiếp; model `Mst_ContractTypeContractNo`). Thêm entity `ContractNumberRule` + enum `Typefix` (Models/Entities.cs) + `ContractType.NumberRule`, DbSet + mapping 1-1 (AppDbContext), `ContractService.NumberRulesAsync/SaveNumberRuleAsync/PreviewNumbersAsync` + `NextCodeAsync/NextNumberAsync` (tạo hợp đồng tự sinh số theo quy tắc của loại, fallback mã mặc định HDyyMM-nnnn khi loại chưa có quy tắc; chống trùng số), action `NumberRules`/`SaveNumberRule`/`PreviewNumbers` + view `NumberRules.cshtml` (bảng quy tắc, form thêm/sửa, ví dụ số) + link nav "Đánh số hợp đồng", seed 2 quy tắc mẫu (tiền tố MB- / hậu tố /DL) + migration bảng `NumberRules` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-23: Port nghiệp vụ **Phê duyệt hợp đồng (Contract_Contract_Approved)** từ nguồn QContract (`WAS_Contract_Contract_Approved` → `Contract_Contract_ApprovedX` trong `QContract.cs`, model `Contract_Checker` với `ApprovedDTimeUTC`, luồng Website `ApprovedAndSign`/`ApprovedSignAndSendMail` ở `Contract_ContractController`). Luật cốt lõi: chỉ người có vai trò **Người kiểm tra** (FlagChecker=1) mới được phê duyệt — người ký (FlagChecker=0) bị từ chối "không có quyền phê duyệt"; phải kiểm tra tuần tự theo Idx khi FlagSeq=1; khi duyệt, hợp đồng ghi nhận `ApprDTimeUTC`/`ApprBy`, chuyển `CheckerStatus` sang ONPROCESS và đánh dấu `ApprovedDTimeUTC` cho người kiểm tra. Thêm trường `Contract.ApprovedAt/ApprovedBy` + `IsApproved`, `ContractChecker.ApprovedAt` + `HasApproved`, enum `HistoryAction.Approved` (Models/Entities.cs), DbContext ignore computed props (AppDbContext), `ContractService.ApproveAsync/ApproveStatsAsync` + record `ApproveStats` (chặn duyệt trùng, chặn khi HĐ đã hủy/kết thúc, ghi nhật ký), action `Approve` + section UI "Phê duyệt hợp đồng" ở `Detail.cshtml` (badge trạng thái, dropdown chọn người kiểm tra, ghi chú), helper `Ui.Action` nhãn "Phê duyệt", seed 1 hợp đồng đã duyệt mẫu + migration cột `ApprovedAt`/`ApprovedBy` (Contracts) + `ApprovedAt` (Checkers) Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `3db5748` đã nằm trong `origin/main`. KHÔNG chạy app để verify runtime (theo yêu cầu).