# DEEPEN-LOG — MiniContract

- 2026-XX-XX: BLOCKED — không thể đọc nguồn `D:\idocNet\2020.6.QContract` (tool read trả 403 ngoài workspace, glob từ chối path ngoài workspace) và terminal delegation hỏng hoàn toàn (mọi lệnh, kể cả `Write-Output "probe"`, đều trả `Terminal delegation failed: terminated`). Không chạy được `dotnet build` để verify, không chạy được git. Không port nghiệp vụ nào để tránh over-claim. Cần môi trường có terminal hoạt động để tiếp tục.
- 2026-09-23: Port nghiệp vụ **Phụ lục hợp đồng (annex)** từ nguồn QContract (`FlagContractAnnex` + `ContractRefNo` trong `Contract_Contract.cs`, `SearchContractRefNo` ở Website controller). Thêm `Contract.IsAnnex/ParentContractId/ParentContractCode` + quan hệ tự tham chiếu (DbContext), `ContractService.CreateAnnexAsync/AnnexesAsync` (chặn tạo PL của PL, chặn PL khi HĐ gốc đã hủy), action `CreateAnnex` + view `CreateAnnex.cshtml`, hiển thị danh sách PL ở `Detail`, badge PL ở `Index`, seed 1 PL mẫu + migration cột Postgres. Build Release: 0 error (2 warning cũ ở SignatureService, không liên quan). Commit `b0ca3d9` đã nằm trong `origin/main`.
- 2026-09-23: Port nghiệp vụ **Ô ký trên hợp đồng (Contract_ContractElement)** từ nguồn QContract (`WAS_Contract_ContractElement_Update` + `WAS_Contract_ContractElement_Calc` + model `Contract_ContractElement`/`Contract_ContractElementSum`, hằng `TConst.ElementType` ELECTRONIC/SHORT/DIGITAL). Thêm entity `ContractElement` + enum `ElementType` (Models/Entities.cs), DbSet + mapping (AppDbContext), `ContractService.ElementsAsync/AddElementAsync/SignElementAsync/ElementStatsAsync` + record `ElementStats`, action `AddElement`/`SignElement` + section UI ở `Detail.cshtml` (bảng ô ký, badge loại, thống kê theo loại, form thêm ô, nút ký ô), seed 3 ô ký mẫu + migration bảng `Elements` Postgres. Đồng thời sửa 2 lỗi build **có sẵn** trong HEAD: class `ContractHistory` trùng lặp (xóa bản cũ dùng `HistoryActionType`) và tham chiếu `PublicSign` thiếu controller — tạo `SignController` + 3 view (`PublicSign`/`Invalid`/`Done`) hoàn thiện luồng link ký công khai. Build Release: **0 error, 0 warning**. Verify runtime (SQLite): `/Contract/Detail/3` render section ô ký + seed, POST `/Contract/SignElement` ký thành công (hiển thị tên người ký), `/Sign/PublicSign` render đúng view.
- 2026-09-23: Port nghiệp vụ **Người kiểm tra hợp đồng (Contract_Checker)** từ nguồn QContract (`WAS_Contract_Checker_Accept` + `Contract_Checker_AcceptX` + `Contract_Checker_CheckDB` + model `Contract_Checker`, hằng `TConst.ContractStatus` PENDING/ONPROCESS). Thêm entity `ContractChecker` + enum `CheckerStatus` (Models/Entities.cs), `Contract.CheckerStatus/Checkers/CheckedCount/AllChecked`, DbSet + mapping (AppDbContext), `ContractService.CheckersAsync/AddCheckerAsync/AcceptCheckAsync/CheckerStatsAsync` + record `CheckerStats` (kiểm tra tuần tự theo Idx khi FlagSeq=1, chặn nhảy cóc; đủ người kiểm tra → hợp đồng chuyển ONPROCESS), action `AddChecker`/`AcceptCheck` + section UI ở `Detail.cshtml` (bảng người kiểm tra, badge trạng thái, form thêm, nút kiểm tra), badge KT ở `Index`, helper `Ui.CheckStatus`, seed 2 người kiểm tra mẫu + migration cột `CheckerStatus` + bảng `Checkers` Postgres. Build Release: **0 error, 0 warning**. Verify runtime (SQLite): `/Contract/Detail/3` render section + seed (2 người kiểm tra, "Đã kiểm tra 1/2"), POST `/Contract/AcceptCheck` ký xong → "2/2" + `CheckerStatus=2` (OnProcess) trong DB, thêm 2 người kiểm tra vào HĐ 4 rồi thử kiểm tra người thứ 2 (Idx=2) khi người thứ 1 chưa xử lý → bị chặn đúng (vẫn 0/2). Commit `d49077d` đã nằm trong `origin/main`.- 2026-09-23: Port nghiệp vụ **Lý do kết thúc hợp đồng (Mst_FinishedContractReason) + Kết thúc hợp đồng** từ nguồn QContract (`Mst_FinishedContractReason_CreateX/_UpdateX/_CheckDB` trong `Master.cs`, model `Mst_FinishedContractReason`, hằng `TConst.ContractFinishType` FINISHED/STOPPED, luồng `Contract_ContractParty_FinishX` trong `zTemp.cs` + action `FinishAndSign` ở Website controller). Thêm entity `FinishedContractReason` + enum `FinishType` (Models/Entities.cs), `ContractStatus.Finished` + trường `Contract.FinishReasonCode/FinishReasonName/FinishDescription/FinishedAt` + `IsFinished`, DbSet + mapping (AppDbContext), `ContractService.FinishReasonsAsync/AddFinishReasonAsync/FinishContractAsync` (chặn kết thúc HĐ đã hủy/đã kết thúc, chặn lý do ngừng hiệu lực, ghi nhật ký), action `FinishReasons`/`AddFinishReason`/`Finish` + view `FinishReasons.cshtml`/`Finish.cshtml`, badge "Đã kết thúc" + nút "Kết thúc hợp đồng" ở `Detail`, filter + badge ở `Index`, nav "Lý do kết thúc", helper `Ui.FinishType`, seed 4 lý do mẫu (2 FINISHED/2 STOPPED) + migration bảng `FinishReasons` + 4 cột kết thúc trên `Contracts` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Verify runtime (SQLite): `/Contract/FinishReasons` render 4 lý do seed, POST `/Contract/AddFinishReason` thêm lý do mới OK, `/Contract/Finish/3` render form chọn lý do, POST `/Contract/Finish` kết thúc HĐ 3 → Detail hiển thị badge "Đã kết thúc" + tên lý do + mô tả + thời điểm + dòng nhật ký, `/Contract?status=5` lọc đúng HĐ đã kết thúc, GET `/Contract/Finish/3` trên HĐ đã kết thúc bị chặn (302 + báo lỗi).
- 2026-09-23: Port nghiệp vụ **Phân quyền hợp đồng (Contract_UserInContract)** từ nguồn QContract (`WAS_Contract_UserInContract_Save` → `Contract_UserInContract_SaveX` trong `zTemp.cs`, model `Contract_UserInContract` + `JoinUserInContract` UI model, action `Decentralized` ở Website `Contract_ContractController` + modal `UserInContractSaveModal` trong `Modal.cshtml`). Luật cốt lõi: lưu phân quyền là **GHI ĐÈ TOÀN BỘ** (delete all phân quyền cũ của hợp đồng rồi insert lại danh sách mới). Thêm entity `ContractUserInContract` (Models/Entities.cs) + `Contract.UserAssignments`, DbSet + mapping (AppDbContext), `ContractService.UserAssignmentsAsync/SaveUserAssignmentsAsync` (chuẩn hoá + loại trùng theo UserCode, chặn khi HĐ đã hủy/kết thúc, ghi nhật ký), action `SaveUserAssignments` + section UI ở `Detail.cshtml` (bảng người dùng được phân quyền, textarea nhập `mã | tên | email` mỗi dòng), seed 2 phân quyền mẫu + migration bảng `UserAssignments` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `a23b5a6` (local; push bị remote từ chối do lock ref — chưa nằm trong origin/main). KHÔNG chạy app để verify runtime (theo yêu cầu).- 2026-09-23: Port nghiệp vụ **Lịch sử gửi hợp đồng (Contract_SendHist)** từ nguồn QContract (`WAS_Contract_SendHist_Add` → `Contract_SendHist_SaveX` insert, `WAS_Contract_SendHist_Get` → `Contract_SendHist_GetX` tra cứu theo ContractCode/BulletinType, luồng gửi lại `ContractReSendHist` → `ResendChannel` ở Website `Contract_ContractController`, hằng `TConst.ChannelType` EMAIL/SMS/ZALO + `Client_BulletinType` CONTRACT/OTP). Thêm entity `ContractSendHist` + enum `ChannelType`/`BulletinType` (Models/Entities.cs) + `Contract.SendHistory`, DbSet + mapping (AppDbContext), `ContractService.SendHistoryAsync/AddSendHistAsync/ResendAsync` (ghi nhận gửi theo kênh + bản tin, gửi lại tạo bản ghi mới giữ vết cũ, chặn khi HĐ đã hủy/kết thúc, ghi nhật ký), action `AddSendHist`/`Resend` + section UI ở `Detail.cshtml` (bảng lịch sử gửi, badge kênh/bản tin, form ghi nhận gửi, nút gửi lại các bản tin đã chọn), seed 3 bản ghi gửi mẫu (2 Email CONTRACT + 1 SMS OTP) + migration bảng `SendHistory` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-23: Port nghiệp vụ **Quy tắc đánh số hợp đồng theo loại (Mst_ContractTypeContractNo + Seq_ContractNo_Get)** từ nguồn QContract (`Seq_ContractNo_Get` → `Seq_ContractNo_GetX` trong `Seq.cs`: ghép `{TypefixInput}{số}` khi PREFIX hoặc `{số}{TypefixInput}` khi POSTFIX, số zero-pad theo `SeqNumberLength` qua `To10Mask`; hằng `TConst.Mst_Typefix` PREFIX/POSTFIX; `Contract_Contract_CountByContractType_Get` đếm hợp đồng theo loại để lấy số kế tiếp; model `Mst_ContractTypeContractNo`). Thêm entity `ContractNumberRule` + enum `Typefix` (Models/Entities.cs) + `ContractType.NumberRule`, DbSet + mapping 1-1 (AppDbContext), `ContractService.NumberRulesAsync/SaveNumberRuleAsync/PreviewNumbersAsync` + `NextCodeAsync/NextNumberAsync` (tạo hợp đồng tự sinh số theo quy tắc của loại, fallback mã mặc định HDyyMM-nnnn khi loại chưa có quy tắc; chống trùng số), action `NumberRules`/`SaveNumberRule`/`PreviewNumbers` + view `NumberRules.cshtml` (bảng quy tắc, form thêm/sửa, ví dụ số) + link nav "Đánh số hợp đồng", seed 2 quy tắc mẫu (tiền tố MB- / hậu tố /DL) + migration bảng `NumberRules` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-23: Port nghiệp vụ **Phê duyệt hợp đồng (Contract_Contract_Approved)** từ nguồn QContract (`WAS_Contract_Contract_Approved` → `Contract_Contract_ApprovedX` trong `QContract.cs`, model `Contract_Checker` với `ApprovedDTimeUTC`, luồng Website `ApprovedAndSign`/`ApprovedSignAndSendMail` ở `Contract_ContractController`). Luật cốt lõi: chỉ người có vai trò **Người kiểm tra** (FlagChecker=1) mới được phê duyệt — người ký (FlagChecker=0) bị từ chối "không có quyền phê duyệt"; phải kiểm tra tuần tự theo Idx khi FlagSeq=1; khi duyệt, hợp đồng ghi nhận `ApprDTimeUTC`/`ApprBy`, chuyển `CheckerStatus` sang ONPROCESS và đánh dấu `ApprovedDTimeUTC` cho người kiểm tra. Thêm trường `Contract.ApprovedAt/ApprovedBy` + `IsApproved`, `ContractChecker.ApprovedAt` + `HasApproved`, enum `HistoryAction.Approved` (Models/Entities.cs), DbContext ignore computed props (AppDbContext), `ContractService.ApproveAsync/ApproveStatsAsync` + record `ApproveStats` (chặn duyệt trùng, chặn khi HĐ đã hủy/kết thúc, ghi nhật ký), action `Approve` + section UI "Phê duyệt hợp đồng" ở `Detail.cshtml` (badge trạng thái, dropdown chọn người kiểm tra, ghi chú), helper `Ui.Action` nhãn "Phê duyệt", seed 1 hợp đồng đã duyệt mẫu + migration cột `ApprovedAt`/`ApprovedBy` (Contracts) + `ApprovedAt` (Checkers) Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `3db5748` đã nằm trong `origin/main`. KHÔNG chạy app để verify runtime (theo yêu cầu).- 2026-09-23: Port nghiệp vụ **Hủy hợp đồng bởi một bên (Contract_ContractParty_Cancel)** từ nguồn QContract (`WAS_Contract_ContractParty_Cancel` → `Contract_ContractParty_CancelX` trong `zTemp.cs`, action `Contract_ContractParty_Cancel`/`Contract_ContractParty_CancelMulti` ở Website `Contract_ContractController`, hằng `TConst.ContractStatus.Canceled` + `TConst.ContractPartyStatus.Canceled` + `FunctionActionType` "PartyCancel"). Luật cốt lõi: hợp đồng phải đang xử lý (chưa hủy/kết thúc); bên hủy phải thuộc hợp đồng; khi hủy, hợp đồng chuyển CANCELED (CancelDTimeUTC) và bên đó chuyển CANCELED (CancelDTimeUTC + CancelBy), ghi nhật ký thao tác "PartyCancel". Thêm enum `PartyStatus` (Models/Entities.cs) + `HistoryAction.PartyCancelled`, trường `ContractParty.Status/CancelledAt/CancelledBy/Remark` + `IsCancelled`, DbContext ignore computed prop (AppDbContext), `ContractService.CancelByPartyAsync` (chặn hủy trùng, chặn khi HĐ đã hủy/kết thúc, ghi nhật ký), action `CancelByParty` + section UI "Hủy bởi một bên" ở `Detail.cshtml` (dropdown chọn bên, ghi chú, badge "Đã hủy" + người/thời điểm hủy ở danh sách bên), helper `Ui.Action` nhãn "Bên hủy", seed 1 hợp đồng đã bị Bên B hủy mẫu + migration cột `Status`/`CancelledAt`/`CancelledBy`/`Remark` (Parties) Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-23: Port nghiệp vụ **Mã OTP xác thực ký hợp đồng (Contract_ContractVerifyOtp)** từ nguồn QContract (`WAS_Contract_ContractVerifyOtp_Save` → `Contract_ContractVerifyOtp_SaveX` + `Contract_ContractVerifyOtp_GetX` trong `QContract.cs`, luồng sinh mã ở `ESignController.ShowModalPopupPartySign_QRCode` — `CUtils.GetRandomHexNumber(6)` + hạn 2 phút). Luật cốt lõi: mã OTP gắn với cặp (ContractCode, UserCodeSign); khi sinh mã mới thì **XOÁ toàn bộ mã cũ của cặp đó rồi ghi mã mới** (delete all + insert); khi xác thực, mã phải còn hiệu lực (FlagActive) và chưa hết hạn (`EndDate >= now`). Thêm entity `ContractVerifyOtp` + enum `OtpState` (Models/Entities.cs), DbSet + mapping (AppDbContext, index theo OrgId+ContractCode+UserCodeSign), `ContractService.VerifyOtpsAsync/GenerateVerifyOtpAsync/VerifyOtpAsync` + helper `RandomHex` (sinh 6 ký tự hex ngẫu nhiên, hạn mặc định 2 phút, chặn khi HĐ đã hủy/kết thúc hoặc bên đã ký, đánh dấu UsedAt khi xác thực thành công, ghi nhật ký), action `GenerateVerifyOtp`/`VerifyOtp` + section UI "Mã OTP xác thực ký" ở `Detail.cshtml` (bảng mã OTP + badge trạng thái Còn hiệu lực/Đã dùng/Hết hạn, form sinh mã theo bên + form xác thực), seed 2 mã OTP mẫu (1 còn hiệu lực + 1 đã dùng) + migration bảng `VerifyOtps` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `15ef3b9` đã nằm trong `origin/main`. KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-23: Port nghiệp vụ **Hợp đồng mẫu (Contract_TempContract)** từ nguồn QContract (`Contract_TempContract_SaveX` + `Contract_TempContract_CheckDB` + `Contract_TempContract_GetX` trong `QContract.cs`, model `Contract_TempContract`, luồng Website `Contract_ContractController.Create(tcontractcode)` — chọn mẫu để soạn nhanh hợp đồng). Luật cốt lõi: mã mẫu (TContractCode) + tên mẫu (TContracName) bắt buộc; tên mẫu **KHÔNG trùng trong cùng Org** (TContracNameExist); loại hợp đồng phải tồn tại & đang hiệu lực (Mst_ContractType_CheckDB); **không xóa mẫu đang được hợp đồng sử dụng** (Contract_Contract.TContractCode). Thêm entity `ContractTemplate` (Models/Entities.cs) + `Contract.TemplateId/TemplateCode` + nav `Template`, DbSet + mapping (AppDbContext, index unique OrgId+Code, FK Type), `ContractService.TemplatesAsync/SaveTemplateAsync/DeleteTemplateAsync/CreateFromTemplateAsync` (soạn HĐ từ mẫu copy loại + nội dung mẫu, ghi nhật ký), action `Templates`/`SaveTemplate`/`DeleteTemplate`/`CreateFromTemplate` + view `Templates.cshtml` (danh mục mẫu + form thêm + nút soạn HĐ/xóa) + `CreateFromTemplate.cshtml` (form soạn HĐ từ mẫu) + link nav "Hợp đồng mẫu", seed 2 mẫu (mua bán / đại lý) + migration bảng `Templates` + cột `TemplateId`/`TemplateCode` (Contracts) Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `a9ea19d` đã nằm trong `origin/main`. KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-XX-XX: Port nghiệp vụ **Nhóm hợp đồng mẫu (Contract_TempGroup) + thuộc tính dùng chung (Contract_Attribute_Group)** từ nguồn QContract (`Contract_TempGroup_GetX`/`_CreateX`/`_UpdateX`/`_DeleteX`/`_CheckDB` trong `QContract.cs`, model `Contract_TempGroup` + `Contract_Attribute_Group` ở Common/Models/QContract, API `ContractTempGroupController.WA_Contract_TempGroup_*`). Luật cốt lõi: mã nhóm (ContractTGroupCode) bắt buộc & KHÔNG trùng khi tạo; khi sửa/xóa mã nhóm phải tồn tại; mỗi thuộc tính phải có mã (AttributeContractCode) + giá trị (AttributeValue) không rỗng; khi cập nhật thì GHI ĐÈ toàn bộ thuộc tính cũ của nhóm (delete all + insert all); xóa nhóm thì xóa kèm toàn bộ thuộc tính. Thêm entity `ContractTemplateGroup` + `ContractAttributeGroup` (Models/Entities.cs), DbSet + mapping (AppDbContext, index unique OrgId+Code, FK Group→Attributes), `ContractService.TemplateGroupsAsync/TemplateGroupAsync/SaveTemplateGroupAsync/DeleteTemplateGroupAsync` (chuẩn hoá + kiểm tra thuộc tính, chặn trùng mã nhóm, ghi đè thuộc tính khi sửa), action `TemplateGroups`/`SaveTemplateGroup`/`DeleteTemplateGroup` + view `TemplateGroups.cshtml` (form thêm nhóm + textarea thuộc tính `mã | giá trị`, bảng danh mục nhóm + badge thuộc tính) + link nav "Nhóm hợp đồng mẫu", seed 2 nhóm mẫu (thương mại / đại lý) + migration bảng `TemplateGroups` + `AttributeGroups` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `1130254` (local; push bị remote từ chối do lock ref — chưa nằm trong origin/main). KHÔNG chạy app để verify runtime (theo yêu cầu).- 2026-09-23: Port nghiệp vụ **Chữ ký số của tổ chức (Mst_OrgCKS)** từ nguồn QContract (`Mst_OrgCKS_CheckDB` + `Mst_OrgCKS_CreateX`/`_UpdateX`/`_DeleteX`/`_GetX` trong `Master.cs`, model `Mst_OrgCKS` ở Common/Models/Customer, API `MstOrgCKSController.WA_Mst_OrgCKS_*`). Luật cốt lõi: khóa nghiệp vụ là cặp **(OrgID, CANumber)** — mỗi tổ chức chỉ có 1 chứng thư cho 1 số CA; khi tạo cặp này KHÔNG được trùng (FlagExistToCheck=No), khi sửa/xóa phải tồn tại (FlagExistToCheck=Yes); sửa là cập nhật từng phần (CAOrg/CAEffDTimeUTCStart/CAEffDTimeUTCEnd/FlagActive). Thêm entity `OrgCertificate` + enum `CertState` (Active/Expired/NotYetValid/Inactive) + `State/IsUsable/StateLabel/ValidityLabel` (Models/Entities.cs), DbSet + mapping (AppDbContext, index unique OrgId+CANumber, ignore computed props), `ContractService.CertificatesAsync/SaveCertificateAsync/DeleteCertificateAsync` (chuẩn hoá CANumber, chặn trùng cặp, kiểm tra hiệu lực đến phải sau hiệu lực từ, ghi UpdatedAt/UpdatedBy khi sửa), action `Certificates`/`SaveCertificate`/`DeleteCertificate` + view `Certificates.cshtml` (thẻ thống kê tổng/còn hiệu lực/hết hạn, form thêm-sửa chứng thư, bảng danh mục + badge trạng thái + nút xóa) + link nav "Chữ ký số tổ chức", seed 2 chứng thư mẫu (1 còn hiệu lực VNPT-CA + 1 đã hết hạn Viettel-CA) + migration bảng `OrgCertificates` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `ea3d5b5` (local; push bị remote từ chối do lock ref — chưa nằm trong origin/main). KHÔNG chạy app để verify runtime (theo yêu cầu).- 2026-09-23: Port nghiệp vụ **Cập nhật ghi chú của một bên trong hợp đồng (Contract_Contract_Party_UpdateRemark)** từ nguồn QContract (`WAS_Contract_Contract_Party_UpdateRemark` → `Contract_Contract_Party_UpdateRemarkX` trong `QContract.cs`, luồng Website `Contract_Contract_Party_UpdateRemark` ở `Contract_ContractController.cs`, hằng `TConst.FunctionActionType.UpdateRemark` = "UPDATEREMARK"). Luật cốt lõi: cập nhật trường `Remark` của một bên (định danh theo hợp đồng + bên), ghi `LogLUDTimeUTC`/`LogLUBy` và ghi nhật ký thao tác "UpdateRemark". Thêm enum `HistoryAction.UpdateRemark` (Models/Entities.cs), `ContractService.UpdatePartyRemarkAsync` (chặn khi bên không thuộc hợp đồng, cho phép xóa ghi chú khi để trống, ghi nhật ký), action `UpdatePartyRemark` + form inline "Lưu ghi chú" trong thẻ từng bên ở `Detail.cshtml` (kèm hiển thị ghi chú hiện có), helper `Ui.Action` nhãn "Sửa ghi chú", seed 1 ghi chú mẫu cho Bên B của hợp đồng đã gửi ký. Cột `Remark` trên `Parties` đã có sẵn từ migration trước nên KHÔNG cần migration mới. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). KHÔNG chạy app để verify runtime (theo yêu cầu).

- 2026-XX-XX: Port nghiệp vụ **Cập nhật file hợp đồng (Contract_Contract_UpdateFilePath)** từ nguồn QContract (`WAS_Contract_Contract_UpdateFilePath` → `Contract_Contract_UpdateFilePathX` trong `QContract.cs`, model `Contract_Contract` với `ContractFileName`/`ContractFilePath`/`ContractFileVersion`). Luật cốt lõi: `ContractFileName` bắt buộc (nếu rỗng → lỗi `Contract_Contract_SaveX_Invalid_ContractFileName`); hợp đồng phải tồn tại; cập nhật `ContractFilePath` + `LogLUDTimeUTC`/`LogLUBy`. Thêm trường `Contract.FileName/FilePath/FileVersion/FileUpdatedAt/FileUpdatedBy` + computed `HasFile` (Models/Entities.cs), `ContractService.UpdateFileAsync` (kiểm tra tên file bắt buộc, ghi nhật ký thao tác), action `UpdateFile` + section UI "File hợp đồng" ở `Detail.cshtml` (bảng thông tin file + badge Đã có/Chưa có file, form cập nhật tên file/đường dẫn/phiên bản), seed file mẫu cho hợp đồng đã hoàn tất + migration 5 cột `FileName`/`FilePath`/`FileVersion`/`FileUpdatedAt`/`FileUpdatedBy` (Contracts) Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `44d314c` (local; push bị remote từ chối do lock ref — chưa nằm trong origin/main). KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-23: Port nghiệp vụ **Cấu hình loại hợp đồng (Mst_ContractTypeDtl)** từ nguồn QContract (`Mst_ContractTypeDtl_CheckDB` + `Mst_ContractTypeDtl_SaveX` + `Mst_ContractTypeDtl_GetX` trong `zTemp.cs`, model `Mst_ContractTypeDtl` ở Common/Models, luồng Website `Mst_ContractTypeController.Edit` + `Contract_ContractController.ChangeContractType` — đọc cấu hình kênh theo loại). Luật cốt lõi: khóa nghiệp vụ là **ContractType** (mỗi loại chỉ có 1 cấu hình); khi tạo cấu hình của loại KHÔNG được trùng (FlagExistToCheck=No), khi sửa/xóa phải tồn tại (FlagExistToCheck=Yes); loại hợp đồng phải tồn tại & đang hiệu lực (Mst_ContractType_CheckDB). Cấu hình gồm: tự sinh số hợp đồng (FlagGenContractNo) + các KÊNH gửi hợp đồng (FlagEmailContract/FlagSMSContract/FlagZaloContract) và KÊNH gửi OTP (FlagEmailOTP/FlagSMSOTP/FlagZaloOTP). Thêm entity `ContractTypeConfig` (Models/Entities.cs) + computed `TypeName`/`ContractChannels`/`OtpChannels`/`ContractChannelsLabel`/`OtpChannelsLabel`, DbSet + mapping (AppDbContext, index unique OrgId+TypeId, FK Type), `ContractService.TypeConfigsAsync/SaveTypeConfigAsync/DeleteTypeConfigAsync` (chặn trùng cấu hình theo loại, cập nhật từng phần khi sửa), action `TypeConfigs`/`SaveTypeConfig`/`DeleteTypeConfig` + view `TypeConfigs.cshtml` (form thêm/sửa cấu hình + bảng danh mục cấu hình, badge kênh gửi HĐ/OTP) + link nav "Cấu hình loại HĐ", seed 2 cấu hình mẫu (thương mại / đại lý) + migration bảng `TypeConfigs` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `85a2e2d` đã nằm trong `origin/main`. KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-24: Port nghiệp vụ **Cấu hình ký của tổ chức (Mst_OrgSignConfig)** từ nguồn QContract (`Mst_OrgSignConfig_CheckDB` + `Mst_OrgSignConfig_CreateX`/`_UpdateX`/`_DeleteX`/`_GetX` trong `Master.cs`, model `Mst_OrgSignConfig` ở Common/Models, hằng `TConst.SignType` REMOTE/SERVER/USBTOKEN, API `MstOrgSignConfigController.WA_Mst_OrgSignConfig_*`). Luật cốt lõi: mỗi bản ghi gắn 1 tổ chức (OrgID) với 1 loại ký (SignType) và các tham số ký (chứng thư CANumber/CAOrg/hiệu lực, ký server ServerSignFilePath/ServerSignPassword, ký từ xa SupplierCode/RemoteSignAgreementUUID/RemoteSignPassCode, hình thức xác thực AuthenCode); khi tạo AutoID KHÔNG được trùng (FlagExistToCheck=No), khi sửa/xóa phải tồn tại (FlagExistToCheck=Yes); **với mỗi cặp (SignType, OrgID) chỉ được có TỐI ĐA 1 bản ghi đang hiệu lực (FlagActive=1)** — nhiều hơn 1 thì lỗi MoreThanOneActive. Thêm entity `OrgSignConfig` + enum `SignType` (Remote/Server/UsbToken) + computed `SignTypeLabel`/`ValidityLabel` (Models/Entities.cs), DbSet + mapping (AppDbContext, ignore computed props), `ContractService.SignConfigsAsync/SaveSignConfigAsync/DeleteSignConfigAsync` (chuẩn hoá OrgCode, kiểm tra hiệu lực đến phải sau hiệu lực từ, chặn trùng cấu hình active theo cặp (SignType, OrgID), cập nhật từng phần khi sửa, ghi UpdatedAt/UpdatedBy), action `SignConfigs`/`SaveSignConfig`/`DeleteSignConfig` + view `SignConfigs.cshtml` (thẻ thống kê tổng/đang hiệu lực/ký từ xa/ký server, form thêm-sửa cấu hình, bảng danh mục + badge trạng thái + nút xóa) + link nav "Cấu hình ký tổ chức", seed 2 cấu hình mẫu (REMOTE + SERVER cho tổ chức HTC) + migration bảng `SignConfigs` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `5b34e88` đã nằm trong `origin/main`. KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-24: Port nghiệp vụ **Người ký của hợp đồng (Contract_ContractUser)** từ nguồn QContract (`Contract_ContractUser_CheckDB` + `Contract_ContractUser_ConfirmX` + `Contract_ContractUser_UpdateFlagSendUserX` trong `QContract.1.cs`/`QContract.cs`, model `Contract_ContractUser` ở Common/Models, hằng `TConst.UserSignSatus` NONE/PENDING/CONFIRMED, API `WAS_Contract_ContractUser_Confirm*`/`_UpdateFlagSendUser`). Luật cốt lõi: khóa nghiệp vụ là bộ ba **(ContractCode, PartyCode, UserCodeSysSign)** — khi tạo KHÔNG được trùng (FlagExistToCheck=No), khi sửa/xác nhận phải tồn tại (FlagExistToCheck=Yes); xác nhận ký → `UserSignSatus=CONFIRMED` + `ConfirmDTimeUTC`/`ConfirmBy`; đánh dấu gửi → `FlagSendUser=1` + `SendDateUTC`/`SendBy` (và hợp đồng PENDING → ONPROCESS). Thêm entity `ContractSigner` + enum `UserSignStatus` (Models/Entities.cs) + `Contract.Signers`/`SignerConfirmedCount`/`AllSignersConfirmed`, DbSet + mapping (AppDbContext, index OrgId+ContractId+PartyCode+UserCodeSysSign, FK Contract/Party), `ContractService.SignersAsync/AddSignerAsync/ConfirmSignerAsync/MarkSignerSentAsync/SignerStatsAsync` + record `SignerStats` (chặn trùng bộ ba, chặn khi HĐ đã hủy/kết thúc, ghi nhật ký), action `AddSigner`/`ConfirmSigner`/`MarkSignerSent` + section UI "Người ký của hợp đồng" ở `Detail.cshtml` (bảng người ký theo bên, badge trạng thái ký/gửi, form thêm + nút Gửi/Xác nhận ký), seed 2 người ký mẫu (1 đã ký + 1 chờ ký) + migration bảng `Signers` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). KHÔNG chạy app để verify runtime (theo yêu cầu).- 2026-09-24: Port nghiệp vụ **Ký hợp đồng bởi một bên (Contract_Contract_PartySign)** từ nguồn QContract (`WAS_Contract_Contract_PartySign` → `Contract_Contract_PartySignX` trong `QContract.cs`, model `Contract_ContractParty` với `UserCodeSign`/`UserNameSign`/`InfoToken`/`SignDateUTC`/`SignBy`/`UserToken`/`ContractFileVersion`, hằng `TConst.ContractPartyStatus.Confirmed` + `TConst.ContractStatus.Confirmed`, `FunctionActionType` "PartySign"). Luật cốt lõi: bên ký phải thuộc hợp đồng (`Contract_ContractParty_CheckDB`, FlagExistToCheck=Yes); hợp đồng phải đang chờ ký (PENDING/ONPROCESS); nếu hợp đồng KHÔNG ở PENDING thì `ContractFileVersion` phải khớp (chống ký bản cũ); nếu không có access token thì `UserToken` phải khớp token của bên; `ContractFileName` bắt buộc; nếu bật kiểm OTP thì mã OTP phải tồn tại và còn hiệu lực (`EndDate >= now`); khi ký: bên → CONFIRMED (ghi `SignDateUTC`/`SignBy`/`UserCodeSign`/`UserNameSign`/`InfoToken`), hợp đồng → ONPROCESS + cập nhật file/phiên bản; đủ TẤT CẢ bên CONFIRMED → hợp đồng CONFIRMED (hoàn tất). Thêm trường `ContractParty.UserCodeSign/UserNameSign/InfoToken/UserToken/SignDateUTC/SignBy/ContractFileVersion` + computed `IsConfirmed` (Models/Entities.cs), `Contract.PartyConfirmedCount/AllPartiesConfirmed`, enum `HistoryAction.PartySigned` (Models/Entities.cs), DbContext ignore computed props (AppDbContext), `ContractService.PartySignAsync/PartySignStatsAsync` + record `PartySignStats` (kiểm tra bên/hợp đồng/phiên bản file/token/OTP, chặn ký trùng, chặn khi HĐ đã hủy/kết thúc, ghi nhật ký), action `PartySign` + section UI "Ký hợp đồng theo bên" ở `Detail.cshtml` (bảng bên + người ký + token + trạng thái, form ký theo bên kèm OTP/token), helper `Ui.Action` nhãn "Bên ký", seed 1 bên đã ký mẫu (bên A) + token cho bên B + migration 7 cột `UserCodeSign`/`UserNameSign`/`InfoToken`/`UserToken`/`SignDateUTC`/`SignBy`/`ContractFileVersion` (Parties) Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `f0485d5` đã nằm trong `origin/main`. KHÔNG chạy app để verify runtime (theo yêu cầu).- 2026-09-24: Port nghiệp vụ **Cập nhật hợp đồng sau phê duyệt (Contract_ContractParty_UpdAfterApproved)** từ nguồn QContract (`WAS_Contract_ContractParty_UpdAfterApproved` → `Contract_ContractParty_UpdAfterApprovedX` trong `zTemp.cs`, luồng Website `Contract_ContractController.Contract_Contract_UpdAfterApproved`, model `Contract_ContractParty` với `ValContract`/`ValPaymented`/`ValRemain`/`ValExchange`/`ContractType`/`ContractTypeName`). Luật cốt lõi: hợp đồng phải tồn tại và đang ở trạng thái ONPROCESS/CONFIRMED (đã gửi/đã duyệt, chưa hủy/kết thúc); bên cập nhật phải thuộc hợp đồng (Contract_ContractParty_CheckDB, FlagExistToCheck=Yes); `ValContract >= 0` và `ValPaymented >= 0` (âm → lỗi InvalidValContract/InvalidValPaymented); cập nhật `ValContract`, `ValExchange = ValContract * CurrencyRate`, `ValPaymented`, `ContractType`/`ContractTypeName`, `Remark`; tính lại `ValRemain = ValContract - ValPaymented`; ghi nhật ký thao tác "PartyUpdAfterApproved". Thêm trường `ContractParty.ValContract/ValPaymented/ValRemain/ValExchange/ContractType/ContractTypeName/ValueUpdatedAt/ValueUpdatedBy` + computed `HasValue`, `Contract.CurrencyRate` + enum `HistoryAction.PartyUpdAfterApproved` (Models/Entities.cs), precision + ignore computed (AppDbContext), `ContractService.UpdateAfterApprovedAsync` (kiểm tra trạng thái/bên/giá trị âm, tính ValExchange/ValRemain, ghi nhật ký), action `UpdateAfterApproved` + section UI "Cập nhật giá trị sau phê duyệt" ở `Detail.cshtml` (bảng giá trị theo bên + form cập nhật), helper `Ui.Action` nhãn "Cập nhật sau duyệt", seed giá trị mẫu cho bên A của hợp đồng đã gửi + migration cột `CurrencyRate` (Contracts) + 8 cột giá trị (Parties) Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `dc6b93d` đã nằm trong `origin/main`. KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-24: Port nghiệp vụ **Cấu hình kênh gửi của tổ chức (Mst_Channel + Mst_ChannelEmail/SMS/Zalo)** từ nguồn QContract (`Mst_Channel_SaveX_New20240312` + `Mst_Channel_CheckDB` + `Mst_ChannelType_CheckDB` trong `MasterData.cs`, model `Mst_Channel`/`Mst_ChannelEmail`/`Mst_ChannelSMS`/`Mst_ChannelZalo` ở Common/Models/QContract, hằng `TConst.ChannelType` EMAIL/SMS/ZALO, API `Mst_ChannelController.WA_Mst_Channel_*`). Luật cốt lõi: khóa nghiệp vụ là **OrgID** (mỗi tổ chức chỉ có 1 cấu hình kênh); `ChannelTypeContract`/`ChannelTypeOTP` phải tồn tại & đang hiệu lực (Mst_ChannelType_CheckDB, FlagExistToCheck=Yes, FlagActiveListToCheck=Active); `ChannelTypeAccessKey` luôn bị ép về **EMAIL**; lưu là **upsert** (chưa có thì tạo, đã có thì cập nhật từng phần), xóa thì bỏ cấu hình. Thêm entity `ChannelConfig` + `ChannelEmailConfig`/`ChannelSmsConfig`/`ChannelZaloConfig` + computed `ContractChannelLabel`/`OtpChannelLabel`/`AccessKeyChannelLabel`/`HasEmail`/`HasSms`/`HasZalo` (Models/Entities.cs), DbSet + mapping 1-1 (AppDbContext, index unique OrgId, ignore computed props), `ContractService.ChannelConfigAsync/SaveChannelConfigAsync/DeleteChannelConfigAsync` (upsert theo OrgID, ép AccessKey=Email, cập nhật từng phần cấu hình con Email/SMS/Zalo), action `ChannelConfigs`/`SaveChannelConfig`/`DeleteChannelConfig` + view `ChannelConfigs.cshtml` (thẻ thống kê kênh HĐ/OTP/AccessKey, form cấu hình kênh + thông số Email/SMS/Zalo, bảng cấu hình hiện có + nút xóa) + link nav "Cấu hình kênh gửi", seed 1 cấu hình kênh mẫu (HĐ=Email, OTP=SMS, AccessKey=Email + thông số Email/SMS/Zalo) + migration bảng `ChannelConfigs`/`ChannelEmails`/`ChannelSms`/`ChannelZalo` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `fec2750` (local; push bị remote từ chối do lock ref — chưa nằm trong origin/main). KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-24: Port nghiệp vụ **Mẫu nội dung gửi (Mst_SubmissionForm + Mst_SubmissionFormMessage + Mst_SubmissionFormZNS)** từ nguồn QContract (`Mst_SubmissionForm_CheckDB` + `Mst_SubmissionForm_SaveX` trong `MasterData.cs`, model `Mst_SubmissionForm`/`Mst_SubmissionFormMessage`/`Mst_SubmissionFormZNS` ở Common/Models/QContract, luồng Website `Mst_SubmissionFormController.Save/Update/Delete`, hằng `TConst.ChannelType` EMAIL/SMS/ZALO + `Client_BulletinType` CONTRACT/OTP). Luật cốt lõi: `SubFormCode` bắt buộc (rỗng → lỗi InvalidSubFormCode) và KHÔNG trùng khi tạo (SubFormCodeExisted); khi lưu, `ChannelType` phải tồn tại & đang hiệu lực (Mst_ChannelType_CheckDB) và `BulletinType` phải tồn tại & đang hiệu lực (Mst_BulletinType_CheckDB); lưu là GHI ĐÈ toàn bộ nội dung (Message) + tham số ZNS của mẫu (delete all + insert all); xóa mẫu thì xóa kèm nội dung + tham số ZNS. Đây là danh mục mà cấu hình kênh gửi (Mst_ChannelEmail/SMS/Zalo — đã port trước đó) tham chiếu qua `SubFormCodeEmailContract/OTP/AccessKey`. Thêm entity `SubmissionForm`/`SubmissionFormMessage`/`SubmissionFormZns` + computed `ChannelLabel`/`BulletinLabel`/`MessageCount`/`ZnsParamCount` (Models/Entities.cs), DbSet + mapping (AppDbContext, index unique OrgId+SubFormCode, FK Form→Messages/ZnsParams, ignore computed props), `ContractService.SubmissionFormsAsync/SubmissionFormAsync/SaveSubmissionFormAsync/DeleteSubmissionFormAsync` (chuẩn hoá nội dung + tham số ZNS, chặn trùng mã mẫu, ghi đè nội dung khi sửa, xóa kèm con), action `SubmissionForms`/`SaveSubmissionForm`/`DeleteSubmissionForm` + view `SubmissionForms.cshtml` (thẻ thống kê tổng/hiệu lực/Email/Zalo, form thêm mẫu + textarea nội dung `tiêu đề | nội dung` + tham số ZNS `mã ZNS | nguồn | mã tham số | giá trị`, bảng danh mục + badge kênh/bản tin + nút xóa) + link nav "Mẫu nội dung gửi", seed 3 mẫu (MAIL-HD, MAIL-OTP, ZALO-HD kèm 2 tham số ZNS) + migration bảng `SubmissionForms`/`SubmissionFormMessages`/`SubmissionFormZns` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `7a71672` đã nằm trong `origin/main`. KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-24: Port nghiệp vụ **Chi tiết hợp đồng (Contract_ContractDtl)** từ nguồn QContract (model `Contract_ContractDtl` ở Common/Models/QContract/Contract_Contract, insert trong `Contract_Contract_SaveX` ở `QContract.cs`, công thức tính tiền ở Website `Views/Contract_Contract/SignMulti.cshtml` dòng 1194-1230: `ValContract = Qty × UnitPrice`, `ValTax = VATRate% × ValContract / 100`, `ValDiscount = ValContract × DiscountRate / 100`). Đây là dòng hàng hóa/dịch vụ của hợp đồng (mã/tên mặt hàng, ĐVT, đơn giá, số lượng, thuế suất, chiết khấu) — nghiệp vụ cốt lõi chưa có trong MiniContract. Thêm entity `ContractDetail` + computed `LineTotal`/`UnitLabel` (Models/Entities.cs) + `Contract.Details`/`DetailTotal`/`DetailTaxTotal`/`DetailDiscountTotal`/`DetailGrandTotal`, DbSet + mapping (AppDbContext, precision 18,2 cho các cột tiền, FK Contract→Details, ignore computed props), `ContractService.DetailsAsync/AddDetailAsync/DeleteDetailAsync/DetailStatsAsync` + record `DetailStats` (tính LẠI các giá trị tiền ở server theo công thức QContract, không tin số client gửi lên; chặn khi HĐ đã hủy/kết thúc; ghi nhật ký), action `AddDetail`/`DeleteDetail` + section UI "Chi tiết hợp đồng (hàng hóa/dịch vụ)" ở `Detail.cshtml` (bảng dòng chi tiết + tfoot tổng cộng, form thêm dòng, nút xóa), seed 3 dòng chi tiết mẫu cho hợp đồng đã gửi ký + migration bảng `Details` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `9548c99` đã nằm trong `origin/main`. KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-24: Port nghiệp vụ **Trường động của hợp đồng (Contract_Attribute_Contract + Contract_Attribute_ContractDtl + Mst_Attribute_Contract)** từ nguồn QContract (model `Contract_Attribute_Contract`/`Contract_Attribute_ContractDtl`/`Mst_Attribute_Contract` ở Common/Models/QContract, luật `Mst_Attribute_Contract_CheckDB`/`_CreateX`/`_UpdateX`/`_DeleteX` + lưu trường động trong `Contract_Contract_SaveX` ở `QContract.cs`, API `MstAttributeContractController.WA_Mst_Attribute_Contract_*`, luồng Website `Contract_ContractController` — `ViewBag.AttList` + `model.AttList`). Đây là nghiệp vụ **thuộc tính động (trường động)** của hợp đồng — cấp hợp đồng (header) và cấp chi tiết (detail) — chưa có trong MiniContract. Luật cốt lõi: mã trường (AttributeContractCode) bắt buộc & KHÔNG trùng khi tạo (FlagExistToCheck=No), khi sửa/xóa phải tồn tại (FlagExistToCheck=Yes); mỗi giá trị trường động phải có mã trường tồn tại & đang hiệu lực trong Mst_Attribute_Contract + giá trị không rỗng; khi lưu trường động của hợp đồng là **GHI ĐÈ TOÀN BỘ** (delete all + insert all). Thêm entity `AttributeContract` (danh mục) + `ContractAttribute` (cấp hợp đồng) + `ContractAttributeDtl` (cấp chi tiết) (Models/Entities.cs) + `Contract.Attributes`/`AttributeDetails`, DbSet + mapping (AppDbContext, index unique OrgId+Code cho danh mục, FK Contract→Attributes/AttributeDetails, ignore computed props), `ContractService.AttributeMastersAsync/SaveAttributeMasterAsync/DeleteAttributeMasterAsync/AttributesAsync/SaveAttributesAsync/AttributeDetailsAsync/SaveAttributeDetailsAsync` (chuẩn hoá + kiểm tra mã trường tồn tại & hiệu lực, chặn trùng mã danh mục, ghi đè toàn bộ khi lưu, chặn khi HĐ đã hủy/kết thúc, ghi nhật ký), action `AttributeMasters`/`SaveAttributeMaster`/`DeleteAttributeMaster`/`SaveAttributes`/`SaveAttributeDetails` + section UI "Trường động hợp đồng" + "Trường động chi tiết" ở `Detail.cshtml` (bảng trường động + textarea `mã | giá trị` và `thứ tự | mã | giá trị`) + view `AttributeMasters.cshtml` (danh mục trường động + form thêm + nút xóa) + link nav "Trường động hợp đồng", seed 3 trường động mẫu (LOAI_HD/THOI_HAN/XUAT_XU) + 2 giá trị cấp hợp đồng + 2 giá trị cấp chi tiết + migration bảng `AttributeContracts`/`ContractAttributes`/`ContractAttributeDtls` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `d28d599` đã nằm trong `origin/main`. KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-24: Port nghiệp vụ **Quản lý thông báo (Mst_NotifyType + Map_UserInNotifyType)** từ nguồn QContract (`Mst_NotifyType_CheckDB`/`_CreateX`/`_UpdateX`/`_DeleteX` + `Map_UserInNotifyType_CheckDB`/`_SaveX` trong `Master.cs`, model `Mst_NotifyType`/`Map_UserInNotifyType` ở Common/Models/QContract, luồng Website `Map_UserInNotifyTypeController`). Đây là nghiệp vụ **danh mục loại thông báo + cài đặt bật/tắt nhận thông báo theo người dùng** — chưa có trong MiniContract. Luật cốt lõi: `NotifyType` bắt buộc khi tạo (rỗng → InvalidNotifyType) và KHÔNG trùng khi tạo (FlagExistToCheck=No), khi sửa/xóa phải tồn tại (FlagExistToCheck=Yes); lưu cài đặt bật/tắt là **GHI ĐÈ theo cặp (UserCode, NotifyType)** — xóa bản ghi trùng cặp rồi insert lại (delete matching + insert all), `FlagNotify` chuẩn hóa về cờ Yes/No. Thêm entity `NotifyType` (mã/mô tả/bật mặc định/hiệu lực + computed `SubscriberCount`) + `UserNotifyType` (UserCode/NotifyTypeCode/FlagNotify + computed `FlagLabel`) (Models/Entities.cs), DbSet + mapping (AppDbContext, index unique OrgId+NotifyTypeCode cho danh mục, index OrgId+UserCode+NotifyTypeCode cho cài đặt, ignore computed props), `ContractService.NotifyTypesAsync/SaveNotifyTypeAsync/DeleteNotifyTypeAsync/UserNotifyTypesAsync/SaveUserNotifyTypesAsync` (chặn trùng mã loại, cập nhật từng phần khi sửa, xóa loại thì xóa kèm cài đặt của loại, ghi đè theo cặp khi lưu cài đặt), action `NotifyTypes`/`SaveNotifyType`/`DeleteNotifyType`/`SaveUserNotifyTypes` + view `NotifyTypes.cshtml` (thẻ thống kê tổng/hiệu lực/bật mặc định/lượt người dùng bật, form thêm loại thông báo, form cài đặt bật/tắt theo người dùng dạng textarea `mã người dùng | mã loại | bật/tắt`, bảng danh mục loại + bảng cài đặt người dùng) + link nav "Quản lý thông báo", seed 3 loại thông báo (CONTRACT_SIGNED/COMPLETED/CANCELLED) + 3 cài đặt người dùng mẫu + migration bảng `NotifyTypes`/`UserNotifyTypes` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `5f4172b` (local; push bị remote từ chối do lock ref — chưa nằm trong origin/main). KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-24: Port nghiệp vụ **Danh mục loại mẫu in (Mst_TempType)** từ nguồn QContract (`Mst_TempType_CheckDB` + `Mst_TempType_CreateX`/`_UpdateX`/`_DeleteX` trong `Temp.cs`, model `Mst_TempType` ở Common/Models, API `WAS_Mst_TempType_Create`/`_Update`/`_Delete`). Đây là nghiệp vụ **danh mục loại mẫu in (print template type)** — chưa có trong MiniContract. Luật cốt lõi: `TempType` bắt buộc khi tạo (rỗng → InvalidTempType) và KHÔNG trùng khi tạo (FlagExistToCheck=No), khi sửa/xóa phải tồn tại (FlagExistToCheck=Yes); `TempTypeName`, `TempSize`, `ImageFilePath` đều bắt buộc (không rỗng); sửa là cập nhật từng phần (TempTypeName/TempTypeDesc/TempSize/ImageFilePath/Remark/FlagActive). Thêm entity `TempType` (mã/tên/mô tả/khổ/ảnh xem trước/ghi chú/hiệu lực + computed `HasImage`) (Models/Entities.cs), DbSet + mapping (AppDbContext, index unique OrgId+Code, ignore computed prop), `ContractService.TempTypesAsync/SaveTempTypeAsync/DeleteTempTypeAsync` (chặn trùng mã khi tạo, cập nhật từng phần khi sửa, kiểm tra các trường bắt buộc), action `TempTypes`/`SaveTempType`/`DeleteTempType` + view `TempTypes.cshtml` (thẻ thống kê tổng/hiệu lực/có ảnh, form thêm loại mẫu in, bảng danh mục + badge trạng thái + nút xóa) + link nav "Loại mẫu in", seed 3 loại mẫu in (A4-PORTRAIT/A4-LANDSCAPE/A5-BOOK) + migration bảng `TempTypes` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `bb2e197` đã nằm trong `origin/main`. KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-24: Port nghiệp vụ **Cập nhật thông tin bên tham gia (Contract_ContractParty_Update) + Ghi nhận gửi mail cho bên (Contract_ContractParty_UpdEmailSend)** từ nguồn QContract (`WAS_Contract_ContractParty_Update` → `Contract_ContractParty_UpdateX` trong `QContract.cs` dòng 44365/44709; `WAS_Contract_ContractParty_UpdEmailSend` → `Contract_ContractParty_UpdEmailSendX` trong `zTemp.1.cs` dòng 581/924; model `Contract_ContractParty` ở Common/Models/QContract; luồng Website `Contract_ContractController` dòng 5670-5690 — gửi mail thông báo cho các bên rồi gọi `WA_Contract_ContractParty_UpdEmailSend`). Luật cốt lõi: `ContractCode` bắt buộc (rỗng → lỗi `Contract_ContractParty_UpdateX`); bên phải thuộc hợp đồng (`Contract_ContractParty_CheckDB`, FlagExistToCheck=Yes); cập nhật thông tin pháp lý/liên hệ của bên (địa chỉ, MST, email, SĐT, website, ngân hàng, người đại diện); khi gửi mail ghi nhận `EmailSend`/`SendEmailDTimeUTC`/`SendEmailBy`. Thêm trường `ContractParty.Address/Website/BankCode/BankName/BankAccountNo/RepresentName/RepresentPosition/InfoUpdatedAt/InfoUpdatedBy` + `EmailSend/SendEmailDTimeUTC/SendEmailBy` + computed `HasInfo`/`EmailSent` (Models/Entities.cs), `ContractService.UpdatePartyAsync/MarkPartyEmailSentAsync/PartyInfoStatsAsync` + record `PartyInfoStats` (chặn khi HĐ đã hủy/kết thúc, chặn bên không thuộc HĐ, mặc định email lấy từ bên, ghi nhật ký), action `UpdateParty`/`MarkPartyEmailSent` + section UI "Thông tin bên tham gia" ở `Detail.cshtml` (bảng thông tin bên + badge Có thông tin/Đã gửi mail, form cập nhật thông tin bên, form ghi nhận gửi mail), seed thông tin + mail gửi mẫu cho 2 bên của HĐ đã gửi ký + migration 12 cột mới trên bảng `Parties` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `3366827` (local; push bị remote từ chối do lock ref — chưa nằm trong origin/main). KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-24: Port nghiệp vụ **Danh mục tỷ giá ngoại tệ (Mst_CurrencyEx)** từ nguồn QContract (`Mst_CurrencyEx_CheckDB` + `Mst_CurrencyEx_Create`/`_Update`/`_Delete`/`_Get` trong `Master.cs`, model `Mst_CurrencyEx` ở Common/Models, API `WAS_Mst_CurrencyEx_*`, luồng Website `Contract_ContractController` — nạp `ViewBag.ListMst_CurrencyEx` để quy đổi giá trị hợp đồng theo tỷ giá). Đây là nghiệp vụ **danh mục tỷ giá ngoại tệ** (mua/bán/quy đổi liên ngân hàng) — chưa có trong MiniContract. Luật cốt lõi: `CurrencyCode` bắt buộc khi tạo (rỗng → lỗi) và KHÔNG trùng khi tạo (FlagExistToCheck=No), khi sửa/xóa phải tồn tại (FlagExistToCheck=Yes); `CurrencyName` bắt buộc (rỗng → InvalidCurrencyName); nếu có `BaseCurrencyCode` thì đồng tiền gốc phải tồn tại (Mst_CurrencyEx_CheckDB); sửa là cập nhật từng phần (CurrencyName/BuyRate/SellRate/InterEx/Remark) + ghi `UpdatedTime`. Thêm entity `CurrencyExchange` + computed `BaseLabel`/`RateLabel`/`HasRate` (Models/Entities.cs), DbSet + mapping (AppDbContext, index unique OrgId+CurrencyCode, precision 18,4 cho BuyRate/SellRate/InterEx, ignore computed props), `ContractService.CurrenciesAsync/SaveCurrencyAsync/DeleteCurrencyAsync` (chặn trùng mã khi tạo, kiểm tra đồng tiền gốc tồn tại, cập nhật từng phần khi sửa), action `Currencies`/`SaveCurrency`/`DeleteCurrency` + view `Currencies.cshtml` (thẻ thống kê tổng/đã có tỷ giá/đồng tiền gốc, form thêm tỷ giá, bảng danh mục + nút xóa) + link nav "Tỷ giá ngoại tệ", seed 3 tỷ giá mẫu (VND gốc + USD + EUR) + migration bảng `Currencies` Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `192bb1c` (local; push bị remote từ chối do lock ref — chưa nằm trong origin/main). KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-24: Port nghiệp vụ **Báo cáo tổng hợp hợp đồng (Rpt_ContractForDashboard)** từ nguồn QContract (`Rpt_ContractForDashboardX` trong `Report.cs` — gom số HĐ + tổng giá trị quy đổi theo các kỳ `ContractCreateDate`/`ContractPending`/`ContractDateInDay`/`ContractDateInWeek`/`ContractDateInMonth`/`ContractDateInYear`; model `Rpt_ContractForDashboard` ở Common/Models/QContract/Contract_Contract; luồng Website `DashboardController.DashboardSummary` + view `Views/Dashboard/DashboardSummary.cshtml`). Đây là nghiệp vụ **báo cáo tổng hợp hợp đồng cho dashboard** — chưa có trong MiniContract. Luật cốt lõi: số HĐ theo ngày/tuần/tháng/năm tính theo ngày tạo; HĐ chờ ký = trạng thái Sent/PartiallySigned; giá trị quy đổi lấy từ tổng `ValExchange` của các bên (fallback giá trị hợp đồng). Thêm record `DashboardReportRow` + `DashboardReport` (có helper `Row/Qty/Value`) + `ContractService.DashboardReportAsync(from, to)` (Models/Entities.cs không đổi — chỉ dùng entity sẵn có), action `Report` + view `Report.cshtml` (thẻ thống kê HĐ tạo trong ngày/chờ ký/trong ngày/trong năm + bảng chi tiết theo kỳ + form lọc khoảng ngày) + link nav "Báo cáo tổng hợp". Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `e8777e5` đã nằm trong `origin/main`. KHÔNG chạy app để verify runtime (theo yêu cầu).
- 2026-09-24: Port nghiệp vụ **Danh mục loại hợp đồng (Mst_ContractType)** từ nguồn QContract (`Mst_ContractType_CheckDB` + `Mst_ContractType_CreateX`/`_UpdateX`/`_DeleteX`/`_GetX` trong `Master.cs` dòng 43978-45470, model `Mst_ContractType` ở Common/Models/QContract, luồng Website `Mst_ContractTypeController` + `LoadDataController.Mst_ContractType_Create/Update`). Đây là nghiệp vụ **danh mục loại hợp đồng** (mã/tên/mô tả/hiệu lực) — MiniContract trước đó chỉ có entity `ContractType` tối giản (Name/Code/BodyTemplate) dùng nội bộ, CHƯA có màn quản lý + luật nghiệp vụ. Luật cốt lõi: `ContractType` bắt buộc khi tạo (rỗng → lỗi) và KHÔNG trùng khi tạo (FlagExistToCheck=No), khi sửa/xóa phải tồn tại (FlagExistToCheck=Yes); `ContractTypeName` bắt buộc; **KHÔNG xóa loại đang được sử dụng** — chặn khi loại đang được hợp đồng mẫu (`Contract_TempContract`) hoặc bên hợp đồng (`Contract_ContractParty`) dùng (lỗi `Mst_ContractType_Delete_ContractTypeUsed`). Thêm trường `ContractType.Description/Active/CreatedAt/CreatedBy` + computed `TemplateCount`/`ContractCount`/`InUse`/`ActiveLabel` (Models/Entities.cs), DbSet mapping (AppDbContext, ignore computed props, index unique OrgId+Code), `ContractService.ContractTypesAsync/SaveContractTypeAsync/DeleteContractTypeAsync` (đếm số mẫu + số HĐ đang dùng loại để biết loại có đang được sử dụng; chặn trùng mã khi tạo, cập nhật từng phần khi sửa, chặn xóa loại đang dùng), action `ContractTypes`/`SaveContractType`/`DeleteContractType` + view `ContractTypes.cshtml` (thẻ thống kê tổng/hiệu lực/đang dùng, form thêm loại, bảng danh mục + badge trạng thái + nút xóa disabled khi loại đang dùng) + link nav "Loại hợp đồng", seed 4 loại hợp đồng kèm mô tả + migration 4 cột `Description`/`Active`/`CreatedAt`/`CreatedBy` + unique index `IX_ContractTypes_OrgId_Code` (ContractTypes) Postgres. Build Release: **0 error** (2 warning cũ ở SignatureService, không liên quan). Commit `31252b1` (local; push bị remote từ chối do lock ref — chưa nằm trong origin/main). KHÔNG chạy app để verify runtime (theo yêu cầu).
